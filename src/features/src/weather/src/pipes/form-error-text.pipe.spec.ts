import { FormErrorTextPipe } from './form-error-text.pipe';

describe('FormErrorTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FormErrorTextPipe();
    expect(pipe).toBeTruthy();
  });
});
